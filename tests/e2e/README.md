# E2E (pendiente)

Coloca aquí pruebas end-to-end (p. ej. Playwright) que levanten la app y validen flujos completos.

Convención sugerida: `tests/e2e/*.spec.ts` ejecutadas con `npx playwright test` cuando se añada la herramienta.
